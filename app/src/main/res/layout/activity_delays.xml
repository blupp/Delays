<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".Delays"
    android:background="#51B46D">


    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="numberSigned"
        android:ems="10"
        android:id="@+id/minutesEditText"
        android:textColor="#ffffffff"
        android:textSize="126sp"
        android:textAlignment="center"
        android:background="#00000000"
        android:text="0"
        android:autoText="false"
        android:gravity="center_horizontal"
        android:layout_above="@+id/minutesLabeltextView"
        android:layout_alignLeft="@+id/saveButton"
        android:layout_alignStart="@+id/saveButton" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="How delayed was your train?"
        android:id="@+id/titleTextView"
        android:textSize="22sp"
        android:textColor="#ffffffff"
        android:layout_alignParentTop="true"
        android:layout_alignRight="@+id/minutesEditText"
        android:layout_alignEnd="@+id/minutesEditText" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="minutes"
        android:id="@+id/minutesLabeltextView"
        android:textSize="22sp"
        android:textColor="#ffffffff"
        android:textAlignment="center"
        android:gravity="center_horizontal"
        android:layout_marginBottom="107dp"
        android:layout_above="@+id/saveButton"
        android:layout_alignLeft="@+id/minutesEditText"
        android:layout_alignStart="@+id/minutesEditText" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Save"
        android:id="@+id/saveButton"
        android:background="#b4ffffff"
        android:textColor="#51B46D"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:textSize="22sp" />
</RelativeLayout>
